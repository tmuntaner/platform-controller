---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: upstreamranchers.platform.rubyrainbows.com
spec:
  group: platform.rubyrainbows.com
  names:
    kind: UpstreamRancher
    listKind: UpstreamRancherList
    plural: upstreamranchers
    singular: upstreamrancher
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UpstreamRancher is the Schema for the ranchers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: UpstreamRancherSpec defines the desired state of UpstreamRancher
            properties:
              kubeConfig:
                description: KubeConfig is a kubeconfig of the rancher's local account
                properties:
                  value:
                    type: string
                  valueFrom:
                    properties:
                      secretKeyRef:
                        properties:
                          key:
                            description: Key of the secret
                            type: string
                          name:
                            description: Name of the secret
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              name:
                description: Name is the name of the rancher
                type: string
            required:
            - kubeConfig
            - name
            type: object
          status:
            description: RancherStatus defines the observed state of UpstreamRancher
            properties:
              state:
                description: State is the state of the account
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
