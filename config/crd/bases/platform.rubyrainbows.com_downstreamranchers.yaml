---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: downstreamranchers.platform.rubyrainbows.com
spec:
  group: platform.rubyrainbows.com
  names:
    kind: DownstreamRancher
    listKind: DownstreamRancherList
    plural: downstreamranchers
    singular: downstreamrancher
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DownstreamRancher is the Schema for the downstreamranchers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DownstreamRancherSpec defines the desired state of DownstreamRancher
            properties:
              name:
                description: Name is the name of the downstream rancher
                type: string
              upstreamRancher:
                description: UpstreamRancher is the name of the upstream rancher
                type: string
            required:
            - name
            - upstreamRancher
            type: object
          status:
            description: DownstreamRancherStatus defines the observed state of DownstreamRancher
            properties:
              state:
                description: State is the state of the account
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
